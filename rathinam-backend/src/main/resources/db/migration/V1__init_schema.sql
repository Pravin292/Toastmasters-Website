-- Rathinam Toastmasters Digital Platform Initial Database Schema Baseline
-- This migration initializes Flyway schema versioning.
-- Future tables (members, meetings, roles, attendance, points, badges, etc.) will be added in subsequent migration files.

SELECT 1;
